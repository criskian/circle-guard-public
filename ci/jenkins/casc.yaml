jenkins:
  systemMessage: "CircleGuard CI/CD — Taller 2 Ingeniería de Software"
  numExecutors: 6
  mode: NORMAL

  securityRealm:
    local:
      allowsSignup: false
      users:
        - id: "admin"
          password: "admin123"
          properties:
            - "apiToken"

  authorizationStrategy:
    loggedInUsersCanDoAnything:
      allowAnonymousRead: false

  globalNodeProperties:
    - envVars:
        env:
          - key: CIRCLE_GUARD_ROOT
            value: /workspace/circle-guard-public
          - key: KUBECONFIG
            value: /var/jenkins_home/.kube/config

tool:
  gradle:
    installations:
      - name: "Gradle-Wrapper"
        home: ""
        properties:
          - installSource:
              installers:
                - gradleInstaller:
                    id: "8.7"

  nodejs:
    installations:
      - name: "Node-20"
        properties:
          - installSource:
              installers:
                - nodeJSInstaller:
                    id: "20.14.0"

jobs:
  - script: >
      pipelineJob('circleguard-auth-service') {
        description('Auth Service CI/CD Pipeline')
        definition {
          cpsScm {
            scm {
              git {
                remote { url('file:///workspace/circle-guard-public') }
                branch('*/master')
              }
            }
            scriptPath('services/circleguard-auth-service/Jenkinsfile')
            lightweight(false)
          }
        }
      }
  - script: >
      pipelineJob('circleguard-identity-service') {
        description('Identity Service CI/CD Pipeline')
        definition {
          cpsScm {
            scm {
              git {
                remote { url('file:///workspace/circle-guard-public') }
                branch('*/master')
              }
            }
            scriptPath('services/circleguard-identity-service/Jenkinsfile')
            lightweight(false)
          }
        }
      }
  - script: >
      pipelineJob('circleguard-form-service') {
        description('Form Service CI/CD Pipeline')
        definition {
          cpsScm {
            scm {
              git {
                remote { url('file:///workspace/circle-guard-public') }
                branch('*/master')
              }
            }
            scriptPath('services/circleguard-form-service/Jenkinsfile')
            lightweight(false)
          }
        }
      }
  - script: >
      pipelineJob('circleguard-promotion-service') {
        description('Promotion Service CI/CD Pipeline')
        definition {
          cpsScm {
            scm {
              git {
                remote { url('file:///workspace/circle-guard-public') }
                branch('*/master')
              }
            }
            scriptPath('services/circleguard-promotion-service/Jenkinsfile')
            lightweight(false)
          }
        }
      }
  - script: >
      pipelineJob('circleguard-notification-service') {
        description('Notification Service CI/CD Pipeline')
        definition {
          cpsScm {
            scm {
              git {
                remote { url('file:///workspace/circle-guard-public') }
                branch('*/master')
              }
            }
            scriptPath('services/circleguard-notification-service/Jenkinsfile')
            lightweight(false)
          }
        }
      }
  - script: >
      pipelineJob('circleguard-dashboard-service') {
        description('Dashboard Service CI/CD Pipeline')
        definition {
          cpsScm {
            scm {
              git {
                remote { url('file:///workspace/circle-guard-public') }
                branch('*/master')
              }
            }
            scriptPath('services/circleguard-dashboard-service/Jenkinsfile')
            lightweight(false)
          }
        }
      }

unclassified:
  location:
    url: http://localhost:8080/
  timestamper:
    allPipelines: true
  ansiColorBuildWrapper:
    globalColorMapName: xterm
  sonarGlobalConfiguration:
    buildWrapperEnabled: false
    installations:
      - name: "SonarQube"
        serverUrl: "http://host.docker.internal:9000"
        credentialsId: ""
        triggers:
          skipScmCause: false
          skipUpstreamCause: false
  mailer:
    smtpHost: "host.docker.internal"
    smtpPort: "1025"
    charset: "UTF-8"
    useSsl: false
    useTls: false
    defaultSuffix: "@circleguard.local"
  extendedEmailPublisher:
    defaultContentType: "text/html"
    defaultRecipients: "admin@circleguard.local"
    defaultReplyTo: "noreply@circleguard.local"
    defaultSubject: "$PROJECT_NAME - Build #$BUILD_NUMBER - $BUILD_STATUS!"
    mailAccount:
      smtpHost: "host.docker.internal"
      smtpPort: "1025"
      useSsl: false
